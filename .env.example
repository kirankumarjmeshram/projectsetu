# Application
APP_URL=http://localhost:3000

# Database — PostgreSQL with Drizzle ORM
DATABASE_URL=postgresql://user:password@localhost:5432/projectsetu

# Integration test database — NEVER run destructive test cleanup against DATABASE_URL
TEST_DATABASE_URL=postgresql://user:password@localhost:5432/projectsetu_test

# Authentication (provider is not selected yet)
AUTH_SECRET=

# Private object storage (provider is not selected yet)
STORAGE_BUCKET=
STORAGE_REGION=

# Future AI integration (provider is not selected yet)
OPENAI_API_KEY=
