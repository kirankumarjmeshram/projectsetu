# Components

Reusable presentational UI belongs under `ui`, application framing under `layout`, and cross-feature compositions under `shared`. Add these directories when real components require them.
